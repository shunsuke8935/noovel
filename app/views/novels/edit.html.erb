<h1>Novels#edit</h1>
<div class="container">
	<div class="row">
		<div class="form-group col-md-6 col-md-offset-3">
            <%= form_for @novel, :url => novel_path do |f| %>
    			<h1>小説情報編集</h1>
    			<tr>小説タイトル<span>（必須）</span>
    				<div class="form-group"><%= f.text_field :title ,class:'form-control'%></div>
                </tr>

                <tr>あらすじ<span>（必須）</span>
                    <div class="form-group"><%= f.text_field :synopsis ,class:'form-control'%></div>
                </tr>

                <tr>ジャンル<span>（必須）</span>
                    <div class="form-group"><%= f.select :genre,[["ファンタジー・冒険・SF", "ファンタジー"], ["恋愛", "恋愛"],  ["ミステリー・サスペンス", "ミステリー"], ["歴史", "歴史"],["ホラー・オカルト", "ホラー"] ], class:'form-control' %>
                    </div>
                </tr>

            <%= f.submit '編集', class:'btn btn-primary' %>
            <% end %>
            <h1>ページを編集</h1>
            <% @pages.each do |page| %>
            <div class="well well-sm">
                <%= link_to page.title, edit_novel_page_path(@novel, page) %>
            </div>
    <% end %>
</div>
        </div>
    </div>
</div>